import { Profile } from './Profile'

/* eslint-disable camelcase */
export class UserProfile {
    first_name!: string
    last_name!: string
    email!:string
    username!: string
    // password!: string
    profile!: Profile
}
